import React from 'react';
import BasicContainer from '../containers/BasicContainer';

const BasicPage = () => (
    <div>
        <div>기본 페이지입니다.</div>
        <BasicContainer />
    </div>
);

export default BasicPage;
